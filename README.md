# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные:

#### Интерфейс IProduct

Содержит данные о товаре.

Поля интерфейса:
`id: string` - идентификатор.
`description: string`- описание.
`image: string`- изображение.
`title: string` - заголовок.
`category: string` - категория.
`price: number | null` -цена.

#### Интерфейс IBuyer

Содержит данные о покупателе.

Поля интефейса:
`payment: TPayment` - вид оплаты.
`email: string` - email.
`phone: string` - телефон.
`address: string`- адрес.

#### Модели данных:

##### Класс ProductCatalog

Xранение товаров, которые можно купить в приложении.

Поля класса:
`private products: IProduct[]` - хранит массив всех товаров.
`private selectedCard: IProduct | null` - хранит товар,выбранный для подробного отображения.

Конструктор:
`constructor(products: IProduct[],selectedCard: IProduct | null)` - инициализирует экземпляр с массивом товаров и выбранным элементом.

Методы класса:
`getProducts():IProduct[]` - получение массива товаров из модели.

`getProductById(id: string): IProduct | undefined` - получение одного товара по его id.

`setSelectedProduct(selectedCard: IProduct): void` - сохранение товара для подробного отображения.

`getSelectedProduct():IProduct | null` - получение товара для подробного отображения.

`setProducts(products: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода.

##### Класс ShoppingCart

Хранение товаров, которые пользователь выбрал для покупки.

Поля класса:

`private products: IProduct[]` - хранит массив товаров,выбранных покупателем для покупки.

Конструктор:

`constructor(products: IProduct[])` - Инициализирует экземпляр с массивом товаров.

Методы класса:

`addProduct(product: IProduct): void` - добавление товаров,которые находятся в корзине.

`removeProduct(id: string): void` - удаление товара, полученного в параметре из массива корзины.

`getProductCount():number` - получение количества товаров в корзине.

`getProducts():IProduct[]` - получение массива товаров,которые находятся в корзине.

`getTotalPrice():  number` - получение стоимости всех товаров в корзине.

`hasProduct(id:string):boolean` -проверка наличия товара в корзине по его id, полученного в параметр метода.

`clearCart():void` - очистка корзины

##### Класс Buyer

Данные покупателя, которые тот должен указать при оформлении заказа.

Поля класса:

`private payment: TPayment` - вид оплаты.
`private email: string `- email.
`private phone: string` - телефон.
`private address: string` - адрес.

Конструктор:

`constructor(data: IBuyer)` -инициализирует экземпляр класса данными покупателя из объекта `IBuyer`.

Методы класса:

`validate(): ValidationResult` - валидация данных.

`getBuyer(): IBuyer` -получение всех данных покупателя.

`clearBuyerData()` - очистка данных покупателя.

### Слой коммуникации.

#### Класс ShopApi

Получения списка товаров и создания заказов.

Поля класса:
`private api: IApi` - используемый для выполнения HTTP‑запросов.

Конструктор класса:

`constructor(api: IApi)` -инициализирует экземпляр класса ShopApi, принимая готовый API.

`getProducts(): Promise<IProductResponse>` - получает список товаров из API.

`createOrder(order: IOrder)`- отправляет данные заказа в API для его создания.

### Слой представления.

#### Интерфейс HeaderData
Описывает структуру данных, которые компонент Header ожидает для отображения.

Поля интерфейса:
 `counter: number` -количество товаров в корзине.


#### Класс Header
Отображает шапку приложения.

Поля класса:
 `protected basketButton: HTMLButtonElement` - счётчик товаров в корзине 
 `protected counterElement: HTMLElement` - кнопка открытия корзины

Конструктор класса:
 `constructor(protected events: IEvents, container: HTMLElement)` -инициализирует эмитирования события basket:open при клике на кнопку корзины,корневой DOM‑элемент, в котором будет отрисован компонент.

Методы класса:
 `set counter(value: number)` - обновляет текстовое содержимое элемента
      

#### Интерфейс GalleryData
Описывает структуру данных, которые компонент Gallery ожидает для отображения.

Поля интерфейса:
 `catalog: HTMLElement[]` - массив DOM‑элементов, которые будут отображены в галерее

#### Класс Gallery
Отображает галерею элементов.

Поля класса:
 `protected catalogeElement: HTMLElement` - контейнер внутри container, куда будут помещены элементы галереи.

Конструктор класса:
 `constructor(container: HTMLElement)` - инициализирует корневой DOM‑элемент, в котором будет отрисован компонент.

Методы класса:
 `set catalog(items: HTMLElement[])` -заменяет всё содержимое контейнера catalogeElement на переданный массив элементов


#### Интерфейс ModalData
Описывает структуру данных, которые компонент Modal ожидает для отображения.

Поля интерфейса:
 `content : HTMLElement`- DOM‑элемент, который будет отображён внутри модального окна
    
#### Класс Modal
Управляет модальным окном: отображает контент внутри него и обрабатывает закрытие. 
Поля класса:
 `protected modalCloseButton: HTMLButtonElement`- кнопка закрытия модального окна.
 `protected modalContent: HTMLElement`- контейнер для отображения контента внутри модального окна.

 Конструктор класса:
 `constructor(protected events: IEvents, container: HTMLElement)` -используется для эмитирования события modal:close при клике на кнопку закрытия, инициализирует корневой DOM‑элемент модального окна, в котором будет отрисован компонент.

 Методы класса:
` set content(items: HTMLElement)`- обновляет контент модального окна.


#### Интерфейс SussesData
Описывает структуру данных, которые компонент Success ожидает для отображения.

Поля интерфейса    
`total:number`-сумма списания в условных единицах («синапсах»), которая будет отображена в описании успешного заказа.
    
#### Класс Succes
Отображает экран успешного оформления заказа с указанием суммы списания и кнопкой закрытия. 
Поля класса: 
`protected description: HTMLElement`- элемент для отображения текста с суммой списания.
`protected closeButton: HTMLButtonElement`-кнопка закрытия экрана  с успешной покупкой.

Конструктор класса:
 `constructor(protected events: IEvents, container: HTMLElement)`-используется для эмитирования события modal:close при клике на кнопку закрытия,иниицализирует корневой DOM‑элемент компонента, в котором будет отрисован экран успешного заказа

Методы класса:
`set total (value: number)`-обновляет текстовое содержимое элемента


#### Класс Card 
Базовый шаблон для создания карточек товаров с общими свойствами
Поля класса:
`protected titleElement: HTMLElement`-элемент для отображения заголовка карточки.
`protected priceElement: HTMLElement`-элемент для отображения цены
`protected idCard!: string`- идентификатор карточки. 

Конструктор класса:
`constructor(container: HTMLElement)`-инициализирует корневой DOM‑элемент карточки, в котором будет отрисован компонент

Методы класса:
`set title(value: string)`-обновляет текстовое содержимое элемента
`set price(value: number)`- обновляет текстовое содержимое элемента
`set id(value: string)`-устанавливает значение идентификатора карточки в поле



#### Интерфейс CardCatalogData
Описывает структуру данных, которые компонент CardCatalog ожидает для отображения.

Поля интерфейса:
`category:string`-категория товара
`title:string`-заголовок карточки товара
`image: string`-изображения товара.
`price: number`-цена товара в синапсах

#### Класс CardCatalog

Поля класса:
`protected CatalogButtons: HTMLButtonElement`-элемент карточки, на который навешивается обработчик клика.
`protected categoryCatalog : HTMLElement`-элемент для отображения категории товара
`protected imageCatalog :  HTMLImageElement`-элемент изображения товара

Конструкто класса:
`constructor(protected events: IEvents, container: HTMLElement)`-используется для эмитирования события card:open при клике на карточку, инициализирует корневой DOM‑элемент карточки в каталоге.

Методы класса:
`set category (value: string)` - устанавливает текст категории в categoryCatalog, динамически обновляет CSS‑класс элемента
`set image (value: string)`- устанавливает атрибут src элемента imageCatalog, загружая изображение по URL.
   

#### Интерфейс CardPreviewData
Описывает структуру данных, которые компонент CardPreview ожидает для отображения.

Поля интерфейса:
`category:string`-категория товара
`title:string`-заголовок карточки товара.
`image: string`- изображение товара
`price: number`-цена товара в синапсах
`text: string`-	текстовое описание товара

#### Класс CardPreview
`protected cardButton: HTMLButtonElement`-кнопка добавления товара в корзину.
`protected categoryCard:HTMLElement`- элемент для отображения категории товара.
`protected imageCard: HTMLImageElement`элемент изображения товара
`protected textCard:HTMLElement`элемент для отображения текстового описания товара

Констурктор класса:
`constructor(protected events: IEvents, container: HTMLElement)`-используется для эмитирования события card:add при клике на кнопку добавления, инициализирует корневой DOM‑элемент карточки предпросмотра.

Методы класса:
`set category(value: string)` -устанавливает текст категории в categoryCard, динамически обновляет CSS‑класс элемента  
`set image(value: string)` -устанавливает атрибут src элемента imageCard, загружает изображение по URL.
`set text(value: string)`- обновляет текстовое содержимое элемента


#### Интерфейс CardBasketData
Описывает структуру данных, которые компонент CardBasket ожидает для отображения.

Поля интерфейса:
`index: number`-порядковый номер товара в корзине
`title: string`-название товара
`price: number`-цена товара в синапсах


##### class CardBasket

Поля класса:
`protected basketButton: HTMLButtonElement`-кнопка удаления товара из корзины.
`protected indexCard: HTMLElement`-элемент для отображения порядкового номера товара в корзине.

Конструктор класса:
`constructor(protected events: IEvents, container: HTMLElement)`-используется для эмитирования события basket:delete при клике на кнопку удаления, инициализирует корневой DOM‑элемент карточки предпросмотра

Методы класса:
`set index(value: number)`-обновляет текстовое содержимое элемента
   


#### Интерфейс BasketData
Описывает структуру данных, которые компонент Basket ожидает для отображения.

Поля интерфейса:
`items: HTMLElement[]`-массив DOM‑элементов, представляющих товары в корзине.
`total: number`-итоговая сумма всех товаров в корзине в синапсах.


#### Класс Basket 
Отображает корзину покупок с перечнем товаров, итоговой суммой и кнопкой открытия полного вида корзины.

Поля класса:
`protected listElement: HTMLElement`-контейнер для отображения списка товаров в корзине
`protected basketButtton: HTMLButtonElement`-кнопка открытия корзины
`protected basketPrice: HTMLElement` -элемент для отображения итоговой суммы заказа.

Конструктор класса:
`constructor(protected events: IEvents, container: HTMLElement)`-используется для эмитирования события basket:open при клике на кнопку корзины, инициализирует корневой DOM‑элемент корзины, в котором будет отрисован компонент.

Методы класса:
`set list(items: HTMLElement[])`-заменяет содержимое контейнера listElement на переданный массив элементов 
`set total(value: number)`- обновляет текстовое содержимое элемента


#### Класс Forms
Базовый шаблон для создания форм с обработкой отправки, валидацией и отображением ошибок.

Поля класса:
`protected submitButton: HTMLButtonElement`-кнопка отправки формы 
`protected errorElement:HTMLElement`- элемент для отображения сообщений об ошибках валидации

Конструктор класса:
`constructor(container: HTMLElement, protected events: IEvents)` -инициализирует корневой DOM‑элемент формы, в котором будет отрисован компонент, используется для эмитирования события forms:submit при отправке формы.

Методы класса:
`protected onSubmit()`-вызывается при успешной отправке формы
`set errors(value: string)`- обновляет текстовое содержимое элемента
`set valid(value: boolean)`- управляет состоянием кнопки отправки:



#### ИНтерфейс OrderData
Описывает структуру данных, которые компонент Order ожидает для отображения и валидации.

Поля интерфейса:
`payment: "cash"|"card"`-способ оплаты: 'cash' (наличные) или 'card' (карта).
`address: string`-адрес доставки. 
`valid: boolean`-валидность формы
`error: string`-сообщение об ошибке валидации


#### Класс Order
Форма оформления заказа с выбором способа оплаты (наличные/карта), вводом адреса и валидацией. 

Поля класса:
`protected cardButton: HTMLButtonElement`-кнопка выбора оплаты картой.
`protected cashButton: HTMLButtonElement`-кнопка выбора оплаты наличными.
`protected addressOrder: HTMLInputElement`-поле ввода адреса доставки

Конструктор класса:
`constructor(protected events: IEvents, container: HTMLElement)`- используется для эмитирования событий order:payment, order:address и forms:submit,инициализирует корневой DOM‑элемент формы заказа.

Методы класса:
`set payment(value: "cash"|"card")`-активирует соответствующую кнопку оплаты
`set address(value: string)`-устанавливает значение поля addressOrder
   


#### Интерфейс ContactsData
Описывает структуру данных, которые компонент Contacts ожидает для отображения и валидации.

Поля интерфейса:
`errors: string`-сообщение об ошибке валидации.
`email: string`-введённый email пользователя.
`phone: string`-введённый номер телефона пользователя
`valid: boolean`- валидность формы

#### Класс Contacts
Компонент формы для ввода контактных данных

Поля класса:
`protected emailInput:HTMLInputElement`-поле ввода email
`protected phoneInput:HTMLInputElement`-поле ввода телефона

Конструктор класса:
`constructor(protected events: IEvents, container: HTMLElement)`-используется для эмитирования событий contacts:email, contacts:phone и forms:submit,инициализирует корневой DOM‑элемент формы контактов. 

Методы класса:   
`set email(value: string)`-устанавливает значение поля emailInput
`set phone(value: string)`-Устанавливает значение поля phoneInput.

#### События приложения.
### `catalog:changed`
Инициализация или обновление каталога.
Действие: рендерит карточки товаров в галерее. Для каждого товара создаётся экземпляр `CardCatalog` с обработчиком клика, который эмиттит событие `card:preview`.
Результат: отображение актуального списка товаров.

### `card:preview`
Клик по карточке товара (`CardCatalog`).
Действие:
  создаёт экземпляр `CardPreview` с данными товара;
  настраивает кнопку: «Купить»/«Удалить из корзины» в зависимости от наличия товара в корзине;
  отображает превью в модальном окне (`modal.open()`).
Результат: показ детальной информации о товаре.

### `card:add`
Нажатие кнопки «Купить»/«Удалить» в `CardPreview`.
Действие:
  если товар уже в корзине — удаляет его (`cart.removeProduct()`);
  иначе — добавляет (`cart.addProduct()`), если у товара есть цена;
  эмиттит `cart:changed` для обновления интерфейса.
Результат: изменение состава корзины.

### `basket:open`
Клик на иконку корзины в шапке (`Header`).
Действие:
  формирует список товаров корзины (`cart.getProducts()`);
  для каждого товара создаёт `CardBasket` с кнопкой удаления;
  рассчитывает общую сумму (`reduce()`);
  отображает корзину в модальном окне.
Результат: открытие корзины с актуальными данными.

### `cart:changed`
Любое изменение корзины (`addProduct`, `removeProduct`, `clearCart`).
Действие:
  обновляет счётчик товаров в шапке (`header.counter`);
  при необходимости перерендеривает корзину (`basket:open`).
Результат: синхронизация UI с состоянием корзины.

### `order:open`
Кнопка «Оформить заказ» в корзине (`Basket`).
Действие:
  создаёт форму заказа `Order`;
  связывает ввод данных с `buyer.setData()`;
  отображает форму в модальном окне.
Результат: начало процесса оформления заказа.

### `order:submit`
Отправка формы заказа (`Order.onSubmit`).
Действие: эмиттит `contacts:open` для перехода к вводу контактных данных.
Результат: переход на шаг ввода контактов.

### `contacts:open`
Источник: событие `order:submit`.
Действие:
  создаёт форму контактов `Contacts`;
  связывает ввод с `buyer.setData()`;
  отображает форму в модальном окне.
Результат: ввод контактных данных покупателя.

### `contacts:submit`
Отправка формы контактов (`Contacts.onSubmit`).
Действие:
  собирает данные заказа: товары, сумма, данные покупателя;
  отправляет запрос `shopApi.createOrder()`;
  при успехе: очищает корзину, обновляет UI, эмиттит `order:success`;
  при ошибке: показывает сообщение «ошибка оплаты».
Результат: завершение заказа или обработка ошибки.

### `order:success`
Успешный ответ от сервера (`createOrder()`).
Действие:
  создаёт экран успеха `Success` с суммой заказа;
  отображает его в модальном окне.
Результат: подтверждение успешного оформления заказа.

### `validation:changed`
Изменение данных в формах (`Order`, `Contacts`).
Действие:
  проверяет валидность полей (адрес, способ оплаты, email, телефон);
  обновляет состояние кнопок (`valid`) и отображает ошибки (`errors`).
Результат: интерактивная валидация форм в реальном времени.

    
